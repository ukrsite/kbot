# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
autoscaling:
  enabled: false
replicaCount: 1
image:
  repository: "ghcr.io/ukrsite"
  tag: "v1.0.2-c87e32a"
  arch: amd64
secret:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
securityContext:
  privileged: true
